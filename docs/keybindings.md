# Keyboard reference

Generated by `mix swarm_code.keymap` from `SwarmCodeCLI.UI.Keymap.Bindings`.
Do not edit by hand: `mix swarm_code.keymap --check` fails when this file
and the table disagree.

Keys are read against one *context* at a time, chosen from where the focus
is and which layer is on top. `?` (or `F1` inside a text field) shows the
same list for the current context on screen. Vim's NORMAL and VISUAL modes
exist only with the vim keymap on (`SWARM_KEYMAP=vim`, or "Vim mode" in
the command palette).

## Composer

### Focus

| Keys | Does |
|---|---|
| `Tab` | Move focus on; from the transcript, into the composer |
| `Shift-Tab` | Move focus back |

### Runs

| Keys | Does |
|---|---|
| `Alt-1` | Switch to the first run tab as drawn |
| `Alt-2` | Switch to the second run tab as drawn |
| `Alt-3` | Switch to the third run tab as drawn |
| `Alt-4` | Switch to the fourth run tab as drawn |

### Act

| Keys | Does |
|---|---|
| `Enter` | Send the draft (in every vim mode) |
| `Alt-Enter` | Queue the draft instead of sending it |

### Layers

| Keys | Does |
|---|---|
| `Ctrl-K` | Command palette; the same chord closes it |
| `Ctrl-G` | Runs dashboard; the same chord closes it |
| `Ctrl-R` | Run palette; the same chord closes it |
| `Ctrl-B` / `Alt-I` | Show or hide the inspector dock |
| `Alt-H` / `Alt-Shift-H` / `Ctrl-Alt-H` / `Ctrl-Alt-Shift-H` | Narrow the inspector dock (add Ctrl for a bigger step) |
| `Alt-L` / `Alt-Shift-L` / `Ctrl-Alt-L` / `Ctrl-Alt-Shift-L` | Widen the inspector dock (add Ctrl for a bigger step) |
| `Alt-0` / `Alt-Shift-0` / `Ctrl-Alt-0` / `Ctrl-Alt-Shift-0` | Reset the inspector dock to its default width |

### Edit

| Keys | Does |
|---|---|
| `Ctrl-O` / `Shift-Enter` | Insert a line break without sending |
| `↑` | Up a line, or up the slash-command list |
| `↓` | Down a line, or down the slash-command list |
| `Ctrl-A` | Move to the start of the line |
| `Ctrl-E` | Move to the end of the line |
| `Ctrl-W` | Delete the word before the cursor |
| `Ctrl-U` | Delete back to the start of the line |
| `Ctrl-Z` | Undo the last edit |
| `Ctrl-Shift-Z` | Redo (needs a terminal that reports Ctrl-Shift) |
| `Ctrl-↑` | Give the composer one more row |
| `Ctrl-↓` | Take one row back from the composer |

### Session

| Keys | Does |
|---|---|
| `F1` | The keyboard help sheet; the same key closes it |
| `Ctrl-C` | Detach from the session and leave it running |
| `Esc` | Step out one level; never navigates history |

## Composer, vim NORMAL

### Vim

| Keys | Does |
|---|---|
| `h` | Left one character |
| `j` | Down one line |
| `k` | Up one line |
| `l` | Right one character |
| `w` | Forward to the start of the next word |
| `b` | Back to the start of the previous word |
| `e` | Forward to the end of the word |
| `0` | Start of the line; after a count, one more digit |
| `^` | First non-blank character of the line |
| `$` | End of the line |
| `G` | End of the draft |
| `g` | g g: start of the draft |
| `1` / `2` / `3` / `4` / `5` / `6` / `7` / `8` / `9` | Repeat the next motion or operator that many times |
| `d` | Delete with a motion; d d deletes the line |
| `c` | Delete with a motion, then INSERT; c c the line |
| `y` | Copy with a motion; y y copies the line |
| `x` | Delete the character under the caret |
| `X` | Delete the character before the caret |
| `D` | Delete to the end of the line |
| `C` | Delete to the end of the line, then INSERT |
| `Y` | Copy the line |
| `s` | Delete the character under the caret, then INSERT |
| `S` | Clear the line, then INSERT |
| `p` | Put the last deleted or yanked text after the caret |
| `P` | Put the last deleted or yanked text before the caret |
| `u` | Undo the last edit |
| `Ctrl-R` | Redo (Ctrl-R is the run palette outside NORMAL) |
| `i` | INSERT at the caret |
| `a` | INSERT after the caret |
| `I` | INSERT at the first non-blank of the line |
| `A` | INSERT at the end of the line |
| `o` | New line below, then INSERT |
| `O` | New line above, then INSERT |
| `v` | VISUAL: motions select, then d, y or c act on it |
| `V` | VISUAL with the whole line selected |

### Focus

| Keys | Does |
|---|---|
| `Tab` | Move focus on; from the transcript, into the composer |
| `Shift-Tab` | Move focus back |

### Runs

| Keys | Does |
|---|---|
| `Alt-1` | Switch to the first run tab as drawn |
| `Alt-2` | Switch to the second run tab as drawn |
| `Alt-3` | Switch to the third run tab as drawn |
| `Alt-4` | Switch to the fourth run tab as drawn |
| `]` | Next inspector tab while the inspector is visible |
| `[` | Previous inspector tab while the inspector is visible |

### Act

| Keys | Does |
|---|---|
| `Enter` | Send the draft (in every vim mode) |
| `Alt-Enter` | Queue the draft instead of sending it |

### Layers

| Keys | Does |
|---|---|
| `Ctrl-K` | Command palette; the same chord closes it |
| `Ctrl-G` | Runs dashboard; the same chord closes it |
| `Ctrl-B` / `Alt-I` | Show or hide the inspector dock |
| `Alt-H` / `Alt-Shift-H` / `Ctrl-Alt-H` / `Ctrl-Alt-Shift-H` | Narrow the inspector dock (add Ctrl for a bigger step) |
| `Alt-L` / `Alt-Shift-L` / `Ctrl-Alt-L` / `Ctrl-Alt-Shift-L` | Widen the inspector dock (add Ctrl for a bigger step) |
| `Alt-0` / `Alt-Shift-0` / `Ctrl-Alt-0` / `Ctrl-Alt-Shift-0` | Reset the inspector dock to its default width |

### Session

| Keys | Does |
|---|---|
| `?` / `F1` | The keyboard help sheet; the same key closes it |
| `Ctrl-C` | Detach from the session and leave it running |
| `Esc` | Step out one level; never navigates history |

## Composer, vim VISUAL

### Vim

| Keys | Does |
|---|---|
| `h` | Left one character |
| `j` | Down one line |
| `k` | Up one line |
| `l` | Right one character |
| `w` | Forward to the start of the next word |
| `b` | Back to the start of the previous word |
| `e` | Forward to the end of the word |
| `0` | Start of the line; after a count, one more digit |
| `^` | First non-blank character of the line |
| `$` | End of the line |
| `G` | End of the draft |
| `g` | g g: start of the draft |
| `1` / `2` / `3` / `4` / `5` / `6` / `7` / `8` / `9` | Repeat the next motion or operator that many times |
| `d` / `x` | Delete the selection |
| `y` | Copy the selection |
| `c` | Delete the selection, then INSERT |

### Focus

| Keys | Does |
|---|---|
| `Tab` | Move focus on; from the transcript, into the composer |
| `Shift-Tab` | Move focus back |

### Runs

| Keys | Does |
|---|---|
| `Alt-1` | Switch to the first run tab as drawn |
| `Alt-2` | Switch to the second run tab as drawn |
| `Alt-3` | Switch to the third run tab as drawn |
| `Alt-4` | Switch to the fourth run tab as drawn |
| `]` | Next inspector tab while the inspector is visible |
| `[` | Previous inspector tab while the inspector is visible |

### Act

| Keys | Does |
|---|---|
| `Enter` | Send the draft (in every vim mode) |
| `Alt-Enter` | Queue the draft instead of sending it |

### Layers

| Keys | Does |
|---|---|
| `Ctrl-K` | Command palette; the same chord closes it |
| `Ctrl-G` | Runs dashboard; the same chord closes it |
| `Ctrl-B` / `Alt-I` | Show or hide the inspector dock |
| `Alt-H` / `Alt-Shift-H` / `Ctrl-Alt-H` / `Ctrl-Alt-Shift-H` | Narrow the inspector dock (add Ctrl for a bigger step) |
| `Alt-L` / `Alt-Shift-L` / `Ctrl-Alt-L` / `Ctrl-Alt-Shift-L` | Widen the inspector dock (add Ctrl for a bigger step) |
| `Alt-0` / `Alt-Shift-0` / `Ctrl-Alt-0` / `Ctrl-Alt-Shift-0` | Reset the inspector dock to its default width |

### Session

| Keys | Does |
|---|---|
| `?` / `F1` | The keyboard help sheet; the same key closes it |
| `Ctrl-C` | Detach from the session and leave it running |
| `Esc` | Step out one level; never navigates history |

## Transcript (main)

### Navigate

| Keys | Does |
|---|---|
| `Alt-←` / `Backspace` | Go back to where you came from |
| `g` | Go-to prefix: g top, G bottom, t / T run tabs |
| `j` / `↓` | Move the selection down |
| `k` / `↑` | Move the selection up |
| `Home` | Select the first item (also g g) |
| `G` / `End` | Select the last item and follow the stream |
| `PgDn` | Scroll one page down |
| `PgUp` | Scroll one page up |
| `Ctrl-D` | Scroll half a page down |
| `Ctrl-U` | Scroll half a page up |
| `Ctrl-E` | Scroll one line down, keeping the selection |
| `Ctrl-Y` | Scroll one line up, keeping the selection |
| `h` / `←` | Collapse the selected item |
| `l` / `→` | Expand the selected item |
| `Space` | Expand or collapse the selected item |

### Focus

| Keys | Does |
|---|---|
| `Tab` | Move focus on; from the transcript, into the composer |
| `Shift-Tab` | Move focus back |
| `i` | Focus the composer (INSERT in vim) |

### Runs

| Keys | Does |
|---|---|
| `Alt-1` | Switch to the first run tab as drawn |
| `Alt-2` | Switch to the second run tab as drawn |
| `Alt-3` | Switch to the third run tab as drawn |
| `Alt-4` | Switch to the fourth run tab as drawn |
| `]` | Next inspector tab while the inspector is visible |
| `[` | Previous inspector tab while the inspector is visible |
| `t` | Open the run inspector over this run |

### Act

| Keys | Does |
|---|---|
| `x` | Stop the current run (asks to confirm) |
| `p` | Pause or continue the current run |
| `m` | Mark the current run as seen |
| `a` | Open the action menu for what is selected |
| `o` | Open the full text of the selected item |
| `Enter` | Activate what is focused |
| `Alt-Enter` | Queue the draft instead of sending it |

### Layers

| Keys | Does |
|---|---|
| `Ctrl-K` | Command palette; the same chord closes it |
| `Ctrl-G` | Runs dashboard; the same chord closes it |
| `Ctrl-R` | Run palette; the same chord closes it |
| `Ctrl-B` / `Alt-I` | Show or hide the inspector dock |
| `Alt-H` / `Alt-Shift-H` / `Ctrl-Alt-H` / `Ctrl-Alt-Shift-H` | Narrow the inspector dock (add Ctrl for a bigger step) |
| `Alt-L` / `Alt-Shift-L` / `Ctrl-Alt-L` / `Ctrl-Alt-Shift-L` | Widen the inspector dock (add Ctrl for a bigger step) |
| `Alt-0` / `Alt-Shift-0` / `Ctrl-Alt-0` / `Ctrl-Alt-Shift-0` | Reset the inspector dock to its default width |

### Session

| Keys | Does |
|---|---|
| `?` / `F1` | The keyboard help sheet; the same key closes it |
| `Ctrl-C` | Detach from the session and leave it running |
| `q` | Close the top layer; with none open, quit |
| `Esc` | Step out one level; never navigates history |
| `P` | Hand off to the plain presenter |

## Inspector

### Navigate

| Keys | Does |
|---|---|
| `Alt-←` / `Backspace` | Go back to where you came from |
| `g` | Go-to prefix: g top, G bottom, t / T run tabs |
| `j` / `↓` | Move the selection down |
| `k` / `↑` | Move the selection up |
| `Home` | Select the first item (also g g) |
| `G` / `End` | Select the last item and follow the stream |
| `PgDn` | Scroll one page down |
| `PgUp` | Scroll one page up |
| `Ctrl-D` | Scroll half a page down |
| `Ctrl-U` | Scroll half a page up |
| `Ctrl-E` | Scroll one line down, keeping the selection |
| `Ctrl-Y` | Scroll one line up, keeping the selection |
| `h` / `←` | Collapse the selected item |
| `l` / `→` | Expand the selected item |
| `Space` | Expand or collapse the selected item |

### Focus

| Keys | Does |
|---|---|
| `Tab` | Move focus on; from the transcript, into the composer |
| `Shift-Tab` | Move focus back |
| `i` | Focus the composer (INSERT in vim) |

### Runs

| Keys | Does |
|---|---|
| `Alt-1` | Switch to the first run tab as drawn |
| `Alt-2` | Switch to the second run tab as drawn |
| `Alt-3` | Switch to the third run tab as drawn |
| `Alt-4` | Switch to the fourth run tab as drawn |
| `]` | Next inspector tab while the inspector is visible |
| `[` | Previous inspector tab while the inspector is visible |
| `t` | Open the run inspector over this run |

### Act

| Keys | Does |
|---|---|
| `x` | Stop the current run (asks to confirm) |
| `p` | Pause or continue the current run |
| `m` | Mark the current run as seen |
| `a` | Open the action menu for what is selected |
| `o` | Open the full text of the selected item |
| `Enter` | Activate what is focused |
| `Alt-Enter` | Queue the draft instead of sending it |

### Layers

| Keys | Does |
|---|---|
| `Ctrl-K` | Command palette; the same chord closes it |
| `Ctrl-G` | Runs dashboard; the same chord closes it |
| `Ctrl-R` | Run palette; the same chord closes it |
| `Ctrl-B` / `Alt-I` | Show or hide the inspector dock |
| `Alt-H` / `Alt-Shift-H` / `Ctrl-Alt-H` / `Ctrl-Alt-Shift-H` | Narrow the inspector dock (add Ctrl for a bigger step) |
| `Alt-L` / `Alt-Shift-L` / `Ctrl-Alt-L` / `Ctrl-Alt-Shift-L` | Widen the inspector dock (add Ctrl for a bigger step) |
| `Alt-0` / `Alt-Shift-0` / `Ctrl-Alt-0` / `Ctrl-Alt-Shift-0` | Reset the inspector dock to its default width |

### Session

| Keys | Does |
|---|---|
| `?` / `F1` | The keyboard help sheet; the same key closes it |
| `Ctrl-C` | Detach from the session and leave it running |
| `q` | Close the top layer; with none open, quit |
| `Esc` | Step out one level; never navigates history |
| `P` | Hand off to the plain presenter |

## Pickers: command palette, run palette, runs dashboard, go-to

### Navigate

| Keys | Does |
|---|---|
| `g` | From the go-to popup, jump to the first item |
| `G` | From the go-to popup, jump to the last item |
| `↓` / `Ctrl-N` | Next result |
| `↑` / `Ctrl-P` | Previous result |
| `PgDn` / `Ctrl-D` | A screenful further down the list |
| `PgUp` / `Ctrl-U` | A screenful further up the list |
| `Home` | The first row of the list |
| `End` | The last row of the list |

### Focus

| Keys | Does |
|---|---|
| `Tab` | Move focus on; from the transcript, into the composer |
| `Shift-Tab` | Move focus back |

### Runs

| Keys | Does |
|---|---|
| `t` | From the go-to popup, the next run tab |
| `T` | From the go-to popup, the previous run tab |
| `Alt-1` | Switch to the first run tab as drawn |
| `Alt-2` | Switch to the second run tab as drawn |
| `Alt-3` | Switch to the third run tab as drawn |
| `Alt-4` | Switch to the fourth run tab as drawn |

### Act

| Keys | Does |
|---|---|
| `Enter` | Activate what is focused |
| `Alt-Enter` | Queue the draft instead of sending it |

### Layers

| Keys | Does |
|---|---|
| `Ctrl-K` | Command palette; the same chord closes it |
| `Ctrl-G` | Runs dashboard; the same chord closes it |
| `Ctrl-R` | Run palette; the same chord closes it |
| `Ctrl-B` / `Alt-I` | Show or hide the inspector dock |
| `Alt-H` / `Alt-Shift-H` / `Ctrl-Alt-H` / `Ctrl-Alt-Shift-H` | Narrow the inspector dock (add Ctrl for a bigger step) |
| `Alt-L` / `Alt-Shift-L` / `Ctrl-Alt-L` / `Ctrl-Alt-Shift-L` | Widen the inspector dock (add Ctrl for a bigger step) |
| `Alt-0` / `Alt-Shift-0` / `Ctrl-Alt-0` / `Ctrl-Alt-Shift-0` | Reset the inspector dock to its default width |

### Session

| Keys | Does |
|---|---|
| `F1` | The keyboard help sheet; the same key closes it |
| `Ctrl-C` | Detach from the session and leave it running |
| `q` | Close the top layer; with none open, quit |
| `Esc` | Step out one level; never navigates history |

## Text fields inside dialogs

### Focus

| Keys | Does |
|---|---|
| `Tab` | Move focus on; from the transcript, into the composer |
| `Shift-Tab` | Move focus back |

### Runs

| Keys | Does |
|---|---|
| `Alt-1` | Switch to the first run tab as drawn |
| `Alt-2` | Switch to the second run tab as drawn |
| `Alt-3` | Switch to the third run tab as drawn |
| `Alt-4` | Switch to the fourth run tab as drawn |

### Act

| Keys | Does |
|---|---|
| `Enter` | Activate what is focused |
| `Alt-Enter` | Queue the draft instead of sending it |

### Layers

| Keys | Does |
|---|---|
| `Ctrl-K` | Command palette; the same chord closes it |
| `Ctrl-G` | Runs dashboard; the same chord closes it |
| `Ctrl-R` | Run palette; the same chord closes it |
| `Ctrl-B` / `Alt-I` | Show or hide the inspector dock |
| `Alt-H` / `Alt-Shift-H` / `Ctrl-Alt-H` / `Ctrl-Alt-Shift-H` | Narrow the inspector dock (add Ctrl for a bigger step) |
| `Alt-L` / `Alt-Shift-L` / `Ctrl-Alt-L` / `Ctrl-Alt-Shift-L` | Widen the inspector dock (add Ctrl for a bigger step) |
| `Alt-0` / `Alt-Shift-0` / `Ctrl-Alt-0` / `Ctrl-Alt-Shift-0` | Reset the inspector dock to its default width |

### Edit

| Keys | Does |
|---|---|
| `←` | Move the cursor left, or pick the previous choice |
| `→` | Move the cursor right, or pick the next choice |
| `Ctrl-O` / `Shift-Enter` | Insert a line break without sending |
| `Ctrl-A` | Move to the start of the line |
| `Ctrl-E` | Move to the end of the line |
| `Ctrl-W` | Delete the word before the cursor |
| `Ctrl-U` | Delete back to the start of the line |
| `Ctrl-Z` | Undo the last edit |
| `Ctrl-Shift-Z` | Redo (needs a terminal that reports Ctrl-Shift) |

### Session

| Keys | Does |
|---|---|
| `F1` | The keyboard help sheet; the same key closes it |
| `Ctrl-C` | Detach from the session and leave it running |
| `Esc` | Step out one level; never navigates history |

## Dialogs

### Navigate

| Keys | Does |
|---|---|
| `j` / `↓` / `→` | Focus the next control |
| `k` / `↑` / `←` | Focus the previous control |
| `PgDn` | Scroll the dialog down a page |
| `PgUp` | Scroll the dialog up a page |
| `Ctrl-D` | Scroll the dialog down half a page |
| `Ctrl-U` | Scroll the dialog up half a page |
| `Home` | Scroll the dialog to the top |
| `G` / `End` | Scroll the dialog to the bottom |

### Focus

| Keys | Does |
|---|---|
| `Tab` | Move focus on; from the transcript, into the composer |
| `Shift-Tab` | Move focus back |

### Runs

| Keys | Does |
|---|---|
| `Alt-1` | Switch to the first run tab as drawn |
| `Alt-2` | Switch to the second run tab as drawn |
| `Alt-3` | Switch to the third run tab as drawn |
| `Alt-4` | Switch to the fourth run tab as drawn |
| `]` | Next inspector tab while the inspector is visible |
| `[` | Previous inspector tab while the inspector is visible |

### Act

| Keys | Does |
|---|---|
| `Enter` | Activate what is focused |
| `Alt-Enter` | Queue the draft instead of sending it |
| `a` | Approve the pending request |
| `d` | Deny the pending request |
| `A` | Approve this request and ones like it |
| `y` | Confirm: yes |
| `n` | Confirm: no, close without doing it |
| `1` / `2` / `3` / `4` / `5` / `6` / `7` / `8` / `9` | Pick a numbered question option |
| `Space` | Tick or untick the focused option |

### Layers

| Keys | Does |
|---|---|
| `Ctrl-K` | Command palette; the same chord closes it |
| `Ctrl-G` | Runs dashboard; the same chord closes it |
| `Ctrl-R` | Run palette; the same chord closes it |
| `Ctrl-B` / `Alt-I` | Show or hide the inspector dock |
| `Alt-H` / `Alt-Shift-H` / `Ctrl-Alt-H` / `Ctrl-Alt-Shift-H` | Narrow the inspector dock (add Ctrl for a bigger step) |
| `Alt-L` / `Alt-Shift-L` / `Ctrl-Alt-L` / `Ctrl-Alt-Shift-L` | Widen the inspector dock (add Ctrl for a bigger step) |
| `Alt-0` / `Alt-Shift-0` / `Ctrl-Alt-0` / `Ctrl-Alt-Shift-0` | Reset the inspector dock to its default width |

### Session

| Keys | Does |
|---|---|
| `?` / `F1` | The keyboard help sheet; the same key closes it |
| `Ctrl-C` | Detach from the session and leave it running |
| `q` | Close the top layer; with none open, quit |
| `Esc` | Step out one level; never navigates history |
