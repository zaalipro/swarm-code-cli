# CLI-local change to test/swarm_code/engine/project_context_test.exs at 6dd8d82, after the rewrite
# rules of provenance/sync-rules.json. Recorded by mix swarm_code.provenance.sync.
--- a/apps/swarm_code_daemon/test/swarm_code/domain/engine/project_context_test.exs
+++ b/apps/swarm_code_daemon/test/swarm_code/domain/engine/project_context_test.exs
@@ -5,6 +5,8 @@
   alias SwarmCode.Domain.Projects.Workspace
 
   setup do
+    # CLI: the global MEMORY.md below is a private temporary one, never the real one.
+    SwarmCode.Domain.TestGlobalDir.isolate!()
     dir = Path.join([System.tmp_dir!(), "swarm_code_pctx", Ecto.UUID.generate()])
     File.mkdir_p!(dir)
     global = Workspace.global_memory_file()
