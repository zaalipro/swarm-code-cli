# CLI-local change to test/swarm_code/engine/prompts_test.exs at 6dd8d82, after the rewrite
# rules of provenance/sync-rules.json. Recorded by mix swarm_code.provenance.sync.
--- a/apps/swarm_code_daemon/test/swarm_code/domain/engine/prompts_test.exs
+++ b/apps/swarm_code_daemon/test/swarm_code/domain/engine/prompts_test.exs
@@ -65,56 +65,6 @@
     assert Prompts.lead(@project, 2, mode: "plan") =~ "PLAN MODE"
   end
 
-  # ------------------------------------------------------------- spec 51 §6.5
-
-  describe "pass45 (spec 51 §6.5)" do
-    alias SwarmCode.Domain.Attachments
-    alias SwarmCode.Domain.Conversations.Message
-
-    # Exactly 1.5 MB on disk, header included: eight of them are the 12 MB
-    # ceiling to the byte, and the ceiling is inclusive.
-    @big 1_500_000
-    @png_header <<0x89, "PNG\r\n", 0x1A, 0x0A, 13::32, "IHDR", 1512::32, 982::32>>
-
-    defp store_big! do
-      binary = @png_header <> :binary.copy("Z", @big - byte_size(@png_header))
-      {:ok, a} = Attachments.store("shot.png", "image/png", Base.encode64(binary))
-      a
-    end
-
-    # `@image_window 6` counted messages only: four 5 MB screenshots in one
-    # message are 32 MB of base64, and six such messages 400/413 on every turn
-    # until six newer image messages push them out.
-    test "the image window has a byte ceiling as well as a count" do
-      attachments = for _i <- 1..24, do: store_big!()
-      on_exit(fn -> Enum.each(attachments, &Attachments.delete(&1["id"])) end)
-
-      messages =
-        attachments
-        |> Enum.chunk_every(4)
-        |> Enum.with_index(1)
-        |> Enum.map(fn {four, i} ->
-          %Message{
-            id: Ecto.UUID.generate(),
-            role: "user",
-            content: "shot #{i}",
-            attachments: four
-          }
-        end)
-
-      converted = Prompts.history_to_messages(messages)
-      assert length(converted) == 6
-
-      carried = Enum.flat_map(converted, &(&1[:images] || []))
-      assert length(carried) == 8
-
-      # 12 MB raw is the ceiling: two messages of four 1.5 MB images fit, the
-      # third does not. The rest degrade to the "omitted" line, as before.
-      assert Enum.take(converted, 4) |> Enum.all?(&(&1[:images] == nil))
-      assert Enum.all?(Enum.take(converted, 4), &(&1.content =~ "[image shot.png omitted]"))
-    end
-  end
-
   # ------------------------------------------------------------- spec 51 §6.6
 
   describe "pass45 (spec 51 §6.6)" do
